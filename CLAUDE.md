# CLAUDE.md — 占い・メンタリング支援Webアプリケーション

## プロジェクト概要

出品者（メンター）が相談者の生年月日を入力し、四柱推命・算命学の命式を瞬時に算出、AIが鑑定テキストのベースや傾聴のヒントを出力するWebアプリケーション（SaaS）。

- **リポジトリ:** https://github.com/mikotomiura/Sityuu-Auto
- **技術スタック:** Python / Streamlit / lunar_python / SQLite / OpenAI API / Anthropic API / Gemini API
- **対象環境:** VPSデプロイ（ConoHa VPS）、ブラウザからアクセス
- **運用形態:** マルチユーザー対応のWebサービス

---

## 作業開始時の必読ドキュメント

作業を開始する前に、必ず以下のドキュメントを順番に読むこと：

1. `docs/glossary.md` — ユビキタス言語定義（用語の共通理解）
2. `docs/functional-design.md` — 機能設計書（何を作るか）
3. `docs/architecture.md` — 技術設計書（どう作るか）
4. `docs/repository-structure.md` — リポジトリ構造定義書（どこに置くか）
5. `docs/development-guidelines.md` — 開発ガイドライン（どう書くか）

---

## 構造化ノート（.steering）

**すべての作業セッションで、作業内容を `.steering/` ディレクトリに記録すること。**

### ディレクトリ命名規則

```
.steering/[YYYYMMDD]-[タスク名]/
```

例: `.steering/20260319-add-fortune-input-form/`

### 各セッションで作成する必須ファイル

| ファイル | 目的 | 必須 |
|---|---|---|
| `requirement.md` | 今回の作業の実装内容・要件を記録 | ✅ |
| `design.md` | 実装アプローチと変更内容を記録 | ✅ |
| `tasklist.md` | 具体的な実装タスクをチェックリスト形式で記録 | ✅ |
| `blockers.md` | ブロッカー（障害・未解決課題）の記録 | オプション |
| `decisions.md` | 重要な設計判断・意思決定の記録 | オプション |

### テンプレート

#### requirement.md
```markdown
# 要件定義: [タスク名]
- 日付: [YYYY-MM-DD]
- 関連Issue/PR: 

## 目的
[このタスクで達成すること]

## 受け入れ条件
- [ ] [条件1]
- [ ] [条件2]

## 対象スコープ
[変更対象のファイル・モジュール]

## スコープ外
[今回は対応しないこと]
```

#### design.md
```markdown
# 設計メモ: [タスク名]
- 日付: [YYYY-MM-DD]

## 実装アプローチ
[選択したアプローチとその理由]

## 変更内容
[具体的な変更の概要]

## 代替案
[検討したが採用しなかったアプローチ]

## 影響範囲
[変更による影響を受けるモジュール]
```

#### tasklist.md
```markdown
# タスクリスト: [タスク名]
- 日付: [YYYY-MM-DD]
- ステータス: 未着手 | 進行中 | 完了

## タスク
- [ ] タスク1
- [ ] タスク2
- [ ] タスク3

## 完了条件
- [ ] テスト通過
- [ ] レビュー済み
```

#### blockers.md（オプション）
```markdown
# ブロッカー記録: [タスク名]
- 日付: [YYYY-MM-DD]

## 未解決
| # | 内容 | 影響度 | 発見日 | 対応方針 |
|---|------|--------|--------|----------|

## 解決済み
| # | 内容 | 解決方法 | 解決日 |
|---|------|----------|--------|
```

#### decisions.md（オプション）
```markdown
# 意思決定記録: [タスク名]
- 日付: [YYYY-MM-DD]

## 決定事項
### [決定タイトル]
- **背景:** [なぜこの決定が必要だったか]
- **選択肢:** [検討した選択肢]
- **決定:** [選択した内容]
- **理由:** [選択の理由]
- **影響:** [この決定による影響]
```

---

## サブエージェント（.claude/agents）

本プロジェクトではサブエージェント・アーキテクチャを導入する。各サブエージェントは `.claude/agents/` 内に定義されている。

### 使用方針

- サブエージェントは**詳細な作業を実行した後、結果を簡潔なレポートとしてまとめ、親エージェントに返す**こと
- 適切なサブエージェントが存在する場合は積極的に委譲すること
- サブエージェントのレポートは `.steering/` 内の該当セッションに記録すること

### 利用可能なサブエージェント

#### 成果物チェック（レビュー）
| エージェント | 説明 |
|---|---|
| `code-reviewer` | コードレビュー：コード品質、命名規則、設計パターンの準拠を検証 |
| `test-analyzer` | テスト結果の分析：テストカバレッジ、失敗パターン、品質指標を分析 |
| `security-checker` | セキュリティチェック：脆弱性、個人情報の取り扱い、API キー管理を検査 |

#### 情報収集（影響範囲分析）
| エージェント | 説明 |
|---|---|
| `impact-analyzer` | 変更の影響範囲を調査し、リスク評価を行う |
| `dependency-checker` | 依存パッケージのバージョン互換性・脆弱性を確認 |
| `file-finder` | プロジェクト内の関連ファイルを検索・特定 |

#### 大量データ処理（テストやビルド実行）
| エージェント | 説明 |
|---|---|
| `test-runner` | テストスイートの実行と結果の集約 |
| `build-executor` | ビルド・パッケージングの実行と結果報告 |
| `log-analyzer` | ログファイルの解析とエラーパターンの特定 |

---

## スラッシュコマンド（.claude/commands）

頻繁に実行するワークフローをコマンド化している。`.claude/commands/` 内の各ファイルを参照。

| コマンド | 目的 |
|---|---|
| `/add-feature` | 新機能の追加（設計→実装→テスト→ドキュメント） |
| `/fix-bug` | バグ修正（調査→原因特定→修正→回帰テスト） |
| `/refactor` | リファクタリング（影響分析→リファクタ→テスト） |
| `/review` | コードレビュー（品質→セキュリティ→パフォーマンス） |
| `/test` | テスト実行（単体→統合→E2E→カバレッジ確認） |
| `/setup` | 開発環境セットアップ（依存関係→設定→初期データ） |

---

## スキル（.claude/skills）

各技術領域のベストプラクティスとコーディング規約を定義している。

| スキル | 対象 |
|---|---|
| `streamlit-standards` | Streamlit UI開発の規約とパターン |
| `python-standards` | Python コーディング規約と型定義ルール |
| `test-standards` | テスト戦略と品質基準 |
| `api-integration` | 外部API（OpenAI/Anthropic）連携の規約 |
| `sqlite-standards` | SQLite データベース設計と操作の規約 |

---

## コーディングルール（概要）

詳細は `docs/development-guidelines.md` を参照。

- **言語:** Python 3.11+
- **型ヒント:** すべての関数に型ヒントを付与
- **フォーマッター:** `ruff format`
- **リンター:** `ruff check`
- **テスト:** `pytest`
- **コミットメッセージ:** Conventional Commits（`feat:`, `fix:`, `docs:`, `refactor:`, `test:`）

---

## 重要な注意事項

- **個人情報の取り扱い:** 相談者の生年月日・悩み・メールアドレスは機密情報。コード内にハードコードしない。ログに出力しない。
- **APIキー管理:** `.env` ファイルで管理し、`.gitignore` に必ず含める。
- **データ保存:** データは SQLite でサーバー上に保存。外部通信は LLM API 呼び出しとSMTP送信のみ。
- **運用形態:** VPS上にデプロイされたWebアプリ（SaaS）。ユーザーは `.env` 等のサーバー設定を触らない。
- **作業記録:** すべての作業セッションで `.steering/` に記録を残すこと。
