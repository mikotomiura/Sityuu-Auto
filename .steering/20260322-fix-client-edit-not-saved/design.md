# 設計メモ: 相談者情報の変更が保存されないバグ修正
- 日付: 2026-03-22

## 実装アプローチ
編集セクション全体を `st.form("client_edit_form")` で囲み、
`st.button` を `st.form_submit_button` に置き換える。

## 注意点
- `st.form` 内ではウィジェット変更時にrerunが発生しない
- そのため「出生時間あり」チェックボックスの動的表示切り替えは動作しない
- 対策: time_input を常に表示し、チェックボックスの値で保存時にNullにするかを判断

## 影響範囲
- `src/pages/03_clients.py` のみ
